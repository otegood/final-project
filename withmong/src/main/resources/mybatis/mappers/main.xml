<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.withmong.dao.MainDao">

	<!--인기순으로  내역 가져오기 -->

  	<select id="getAvglikeList" parameterType="Criteria" resultType="Product">
	 	<![CDATA[
	 	 select * from ( select row_number() over (order by avglike desc) as rn , title, price, regdate, tag, no from product_table )  where rn >= #{beginIndex} and rn < #{endIndex}+1 
		]]>
  	</select>

	<!-- 등록순으로 내역 가져오기 -->
  	<select id="getRegList" parameterType="Criteria" resultType="Product">
	 	<![CDATA[
	 	 select * from ( select row_number() over (order by regdate desc) as rn , title, price, regdate, tag, no from product_table )  where rn >= #{beginIndex} and rn < #{endIndex}+1 
		]]>
  	</select>
	

	
	<!-- 조회순으로 내역 가져오기 -->

  	<select id="getHitList" parameterType="Criteria" resultType="Product">
	 	<![CDATA[
	 	 select * from ( select row_number() over (order by hits desc) as rn , title, price, regdate, tag, no from product_table )  where rn >= #{beginIndex} and rn < #{endIndex}+1 
		]]>
  	</select>

	
	
	<!-- 상품 총 개수 구하기? -->
	<select id="getTotalRows" parameterType="Criteria" resultType="int">
		  select count(*) from product_table
	</select>

  	
  	
</mapper>
